# environment
- Runs the project on Windows (PowerShell, `D:\` paths); prefers Windows-compatible commands and file paths in instructions. Confidence: 0.6
- Not deeply familiar with backend/ops tooling — e.g., didn't know dependencies must be installed before `npm run dev` or that tables must be created via the schema script before login/seed works, and asked whether a live site can simply switch domains (unsure what DNS/SSL/redirect changes entail); prefers/benefits from explicit step-by-step setup and troubleshooting instructions with concrete commands and expected output messages. Confidence: 0.7
- Self-hosts the production website on a custom domain they register and manage themselves (previously hosted as heroingredient.com, moving to indonesiaingredient.com), rather than a managed PaaS; expects the assistant to locate domain/brand references in code and clearly separate code-side changes from external registrar/hosting-panel tasks (DNS, SSL, 301 redirect, payment-gateway domain whitelist) when a domain changes. Confidence: 0.5
- When step-by-step instructions fail to resolve an issue (e.g., same DB error persists), prefers the assistant to directly execute the setup/fix commands on their machine (run `db:schema`, seed admin, inspect tables, etc.) rather than only re-explaining the steps. Asks the assistant to fix AND configure things for them (e.g., fill in empty `.env` values like JWT_SECRET by generating the secret and writing it), not just to give guidance. Confidence: 0.75
- Wants the assistant to keep working until the problem is fully resolved and usable, not stop at diagnosis or a partial fix ("bantu aku sampe bisa" — help me until it works): stay engaged through reproducing the error, requesting the missing credential/secrets from the user, applying the fix, and verifying success end-to-end. Confidence: 0.7
- Midtrans environment for this account cannot be inferred from key prefix: the user's keys lack the `SB-` prefix yet are only accepted by the sandbox API (same key returns 401 on `api.midtrans.com` but works on `api.sandbox.midtrans.com`), with production locked until account activation/verification — so payment mode must be forced via explicit env flags (`MIDTRANS_IS_PRODUCTION=false`, `PUBLIC_MIDTRANS_IS_PRODUCTION=false`) and verified by calling the gateway endpoints, never assumed from key format. Confidence: 0.7
- Pastes requested credentials/secrets into chat raw (plain, unformatted) without checking environment or format — selected sandbox but supplied production keys — so the assistant must validate the key prefix/environment and clean up `.env` parsing issues (stray spaces/quotes) rather than trusting the stated choice. Confidence: 0.6
- Deploys via **Coolify** (self-hosted PaaS on their own server) rather than a managed host/Vercel-like service: expects the project prepared for it — Dockerfile-based builds run from the repo root with a combined frontend+backend image, all config passed as Coolify Environment Variables / Build Arguments (the `.env` file is gitignored and does not reach the image), a named port, Persistent Storage volumes for uploads, a domain with auto HTTPS, and one-time DB init commands run in the container terminal. Confidence: 0.6
- Prefers a single-container, same-origin deployment (one Express service that serves the Astro build and the API on the same domain) over separate frontend/backend containers, and wants deploys to self-migrate on boot — schema/seed run automatically from the container entrypoint, gated by env flags — rather than manual one-off migration commands in the container terminal. Confidence: 0.75
- Their Windows machine has `NODE_ENV=production` set globally, so a plain `npm install` silently skips devDependencies (e.g. nodemon, leaving `node_modules/.bin` missing it and `npm run dev` failing with "'nodemon' is not recognized") — remember to install with `npm install --include=dev` or set `NODE_ENV=development` for the session instead of blaming the scripts. Confidence: 0.7
- When they report a feature "still doesn't work" after doing their part, expects the assistant to stop re-explaining and actually reproduce/verify the real user flow end-to-end (e.g. driving the admin UI in a real browser), not just inspect the DB/API — the repeated complaint "udh aku tambahin tapi belum muncul, cek nyambung ke database engga trs coba" turned out to be a genuine UI bug where a prominent save button silently dropped entered data. Confidence: 0.55
- Often brings a deployment/ops problem as a raw pasted artifact rather than a clear question: either a wall of generic troubleshooting steps (frequently generated by another AI) plus their own guess, e.g. "apa karena di projek ini gaada dockerfile?", or the full Coolify build/deploy log ending in a terse "bantu aku menangani ini" — and expects the assistant to pinpoint the real root cause and fix it instead of walking that checklist — so verify the actual repo/deploy state (git remote URL, tracked file paths, committed file contents, and which commit the failed deploy used) before answering, since their local git remote can lag behind the repo Coolify actually builds from (repo renamed Magna-Ingredients → Hero-Ingredients, and the project's Dockerfile lives at Backend/Dockerfile, not the repo root). Confidence: 0.6
- Typically keeps their own dev server (nodemon) running in a terminal while the assistant edits the code, so port 5000 is already in use and the assistant's attempt to start a background server fails (exit 1) — rely on their already-running nodemon to hot-reload new backend code instead of starting a second server. Confidence: 0.45
- Local MySQL comes from XAMPP on Windows (`C:\xampp\mysql\bin\mysqld.exe` / `mysql.exe -u root`), and the project's database is named `magna_ingredients` (kept even though the project was renamed Magna-Ingredients → Hero-Ingredients); expect to start MySQL manually (e.g. `mysqld.exe --defaults-file=...my.ini`) before the Express backend works, and verify connectivity via port 3306 / the mysql CLI rather than assuming it's running. Confidence: 0.55
- Their Windows machine has no Docker Desktop / `docker` CLI and no WSL, so container-level checks (`docker compose up --build`, image builds) can't be run locally — validate whatever is possible without Docker (`bash -n` on shell scripts, the frontend `npm run build`, Vite env-precedence probes), then hand the container test off to them and state plainly that it remains unverified. Confidence: 0.55
- The live deployment can lag the repo: when they report a runtime error they often mean the currently hosted site (one live Coolify deploy was still the frontend-only nginx image — `/api` and `/admin` returned nginx 404, the title was the old "Magna Ingredients", product images were `.jpg`), so fetch the live domain and its baked JS bundle to establish what is actually deployed before debugging local code. Confidence: 0.55
- Local dev needs two servers up at once (Backend Express on :5000 plus the Astro website/admin on :4321), and they often have only the frontend running — so a recurring "Failed to fetch" in the admin panel is usually just the backend process being down, not a code or data bug: check which port is listening (and confirm the rows are still in MySQL) before touching code, and remind them both terminals must run. Confidence: 0.5
json && npm install`) and accepting that versions then follow the `package.json` ranges instead of the lockfile — warn them about that tradeoff rather than pretending the lockfile still pins. Confidence: 0.5
- Local dev needs two servers up at once (Backend Express on :5000 plus the Astro website/admin on :4321), and they often have only the frontend running — so a recurring "Failed to fetch" in the admin panel is usually just the backend process being down, not a code or data bug: check which port is listening (and confirm the rows are still in MySQL) before touching code, and remind them both terminals must run. Confidence: 0.5
