# e-commerce
- When they report the Telegram order notification "masih ga masuk" (still not arriving), don't assume a code bug: they often test with payments that were never actually completed (QRIS left unscanned, orders still `pending`/expired in Midtrans sandbox), so verify the real prerequisites first — prove the bot + target chat work (getMe/getChat/sendMessage) and check the order's raw gateway transaction status — and explain the cause with that evidence before changing code. Confidence: 0.5
- Wants the whole order lifecycle mirrored out-of-app to their own Telegram (configured via `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` env vars, creds kept pre-filled locally) so the merchant gets an instant message (order id, total, customer, items, shipping) not just a dashboard status: payment received (wired to the Midtrans webhook), plus — after asking for more — a 🆕 new-order notification at checkout and a 🔄 status-update message when the admin changes an order's status (processed/shipped/completed/cancelled). Confidence: 0.7
- Storefront browsing must remain open to guests (no login needed to look around), but checkout now requires a customer account: clicking "Bayar Langsung Sekarang" while logged out redirects to a login/register page, and payment only proceeds after login — replacing the earlier no-account/guest-checkout model; customer tokens/roles are kept separate from admin accounts. Confidence: 0.85
- Expects a web-based admin panel for managing the store — not raw API calls (Postman), seed scripts, or manual DB edits. Coverage now spans admin login, product entry/CRUD, and order list with payment status, plus new store features managed from dedicated admin UI pages (shipping couriers with fixed admin-set rates, voucher/discount codes, order detail showing shipping & discount breakdown). Confidence: 0.75
- Expects anything created/edited through the admin panel to show up on the public storefront and reports admin⇄storefront mismatches as bugs ("nambahin produk di admin tapi ga muncul di website pengguna"); wants those explained by which piece of data is still missing (e.g. a Jenis Olahan saved without any Kemasan/Harga variant is deliberately filtered out of the storefront catalog) rather than as a code bug — but keeps pushing back when the data genuinely should have saved, explicitly asking the assistant to verify that the admin panel is actually connected to the database and to try the flow itself instead of just re-explaining. Confidence: 0.6
- Expects the admin panel UI itself to surface status/warnings rather than relying on someone explaining the cause: asks for "notif atau keterangan di website admin biar kita tau" — per-product status badges (visible vs not visible on storefront), an inline warning on each incomplete item (e.g. Jenis without Kemasan/Harga) with the next step to fix it, and a summary banner listing affected products. Confidence: 0.6
- Treats Indonesian Rupiah as the single source of truth for catalog prices: in admin forms the user wants to enter only the IDR price ("aku masukin yang indonesia aja trs harga yang lainya menyesuaikan") and have USD/AED/EUR auto-fill from it, while still remaining manually editable — conversion should reuse the project's existing fixed rates (IDR÷18.000 USD, ÷5.000 AED, ÷21.000 EUR) rather than introducing a new/external rate source. Confidence: 0.7
- Prefers the admin dashboard and the buyer storefront built/deployed together on a single hosting/domain (same-origin, e.g. `/admin` UI + `/api` behind one server) rather than as separate apps; when discussing hosting, expects the admin to automatically ride along to the same hosting as the website. Confidence: 0.7
- Wants image fields in the admin panel to accept a real file upload picked from their computer (upload button + thumbnail preview + upload status/progress note), not only a pasted image URL — the manual URL input stays as a fallback, and the uploaded result must preview correctly before saving. Confidence: 0.55
- Wants storefront-readiness warnings in the admin essentially gone: first asked to delete the repeated per-item inline warning ("⚠ Belum ada kemasan & harga — jenis ini belum tampil di storefront…"), then also asked to delete the summary banner at the top of the list ("⚠ 1 produk belum tampil di storefront…") — at most a compact per-product status chip is tolerated, no explanatory warning text in the product list. Confidence: 0.65
in the admin minimal and non-repetitive: asked to delete the repeated per-item inline warning ("⚠ Belum ada kemasan & harga — jenis ini belum tampil di storefront…"), preferring the compact per-product status badge plus the single summary banner at the top of the list over duplicated explanatory text on every row. Confidence: 0.5
er at the top of the list over duplicated explanatory text on every row. Confidence: 0.5
